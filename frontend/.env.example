# Absolute URL of the Hono API server (no trailing slash).
# Vite bakes this into the static build at build time.
# Dev (PM2):  http://localhost:3300
# Prod:       https://your-api-domain.example.com
VITE_API_URL=http://localhost:3300
