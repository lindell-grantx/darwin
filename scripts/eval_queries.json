[
  {
    "text": "How do I create an Atlas Vector Search index for embeddings stored under embeddings.voyage_4?",
    "ground_truth": "Create an Atlas Search vector index whose definition has a vector field at the nested path embeddings.voyage_4. Set numDimensions to match the embedding model and choose the similarity metric, such as cosine.",
    "expected_facts": [
      "Atlas Search vector index",
      "path embeddings.voyage_4",
      "type vector",
      "numDimensions matches model",
      "similarity metric"
    ],
    "difficulty": "easy",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "Create a MongoDB $vectorSearch query that returns the top 10 matching chunks for a user question.",
    "ground_truth": "Use an aggregation pipeline with a $vectorSearch stage that names the vector index, supplies the queryVector, points path at the embedding field, and sets limit to 10. numCandidates should be larger than limit to give the search room to find good candidates.",
    "expected_facts": [
      "$vectorSearch stage",
      "index name",
      "queryVector",
      "path to embedding field",
      "limit 10",
      "numCandidates greater than limit"
    ],
    "difficulty": "easy",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "Why does Atlas Vector Search need the embedding dimensions in the index definition?",
    "ground_truth": "The vector index must know the fixed length of vectors stored in the indexed field so it can validate and compare them correctly. The numDimensions value must match the embedding model that produced the vectors.",
    "expected_facts": [
      "fixed vector length",
      "numDimensions",
      "matches embedding model",
      "validates vectors",
      "compares vectors correctly"
    ],
    "difficulty": "easy",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "How would you combine vector search with a metadata filter for only MongoDB documentation chunks?",
    "ground_truth": "Add a filter inside the $vectorSearch stage using indexed metadata fields such as metadata.source or metadata.tags. The filter restricts candidate documents before returning the nearest vector matches.",
    "expected_facts": [
      "$vectorSearch filter",
      "metadata.source",
      "metadata.tags",
      "restricts candidates",
      "nearest vector matches"
    ],
    "difficulty": "medium",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "How can MongoDB change streams trigger Darwin's evolution loop after fitness scores are written?",
    "ground_truth": "Watch the fitness_evaluations collection with a change stream and react to insert events. When enough evaluations arrive for a generation, the conductor can compute selection, create children, and update generation summaries without polling.",
    "expected_facts": [
      "watch fitness_evaluations",
      "change stream",
      "insert events",
      "enough evaluations",
      "compute selection",
      "without polling"
    ],
    "difficulty": "medium",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "What's the point of storing generation fitness history in a MongoDB time-series collection?",
    "ground_truth": "Generation summaries are time-ordered measurements, so a time-series collection is a natural fit. It makes trend queries and demo charts for best fitness, mean fitness, and diversity over time straightforward.",
    "expected_facts": [
      "time-ordered measurements",
      "time-series collection",
      "best fitness",
      "mean fitness",
      "diversity over time",
      "trend queries"
    ],
    "difficulty": "easy",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "How should Darwin index the queries collection so agents can sample by domain and difficulty?",
    "ground_truth": "Create secondary indexes on fields used for filtering, especially domain_tags and difficulty. A compound index can help if the system often queries by both fields together.",
    "expected_facts": [
      "secondary indexes",
      "domain_tags",
      "difficulty",
      "compound index",
      "filtering"
    ],
    "difficulty": "easy",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "Why use MongoDB documents for Darwin genomes instead of a fixed SQL table?",
    "ground_truth": "Genome documents contain nested retrieval, coordination, durability, fitness, and mutation fields that may evolve during the hackathon. MongoDB's document model stores that flexible nested structure without forcing every gene into a rigid table layout.",
    "expected_facts": [
      "nested genome fields",
      "retrieval genes",
      "coordination genes",
      "durability genes",
      "flexible document model",
      "rigid table layout"
    ],
    "difficulty": "medium",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "How do transactions help when Darwin updates a population after selection?",
    "ground_truth": "A transaction can keep related writes consistent, such as retiring weak genomes, inserting child genomes, and writing the generation summary. If one write fails, the population is not left half-updated.",
    "expected_facts": [
      "transaction",
      "related writes",
      "retire weak genomes",
      "insert child genomes",
      "generation summary",
      "consistent population"
    ],
    "difficulty": "medium",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "What can go wrong if the vector index path does not match the field used by the retriever?",
    "ground_truth": "The retriever may query a path that is not indexed or that contains vectors with the wrong dimensions. That can produce empty results, query errors, or poor retrieval even when the chunks have valid embeddings elsewhere.",
    "expected_facts": [
      "index path mismatch",
      "retriever path",
      "wrong dimensions",
      "empty results",
      "query errors",
      "poor retrieval"
    ],
    "difficulty": "hard",
    "domain_tags": ["mongodb", "vector-search"]
  },
  {
    "text": "Which Voyage embedding model should I use if I care more about latency than maximum retrieval quality?",
    "ground_truth": "Use a smaller Voyage embedding model such as voyage-4-lite or voyage-4-nano when latency and cost matter more than peak quality. Larger models generally trade more compute for better representation quality.",
    "expected_facts": [
      "voyage-4-lite",
      "voyage-4-nano",
      "lower latency",
      "lower cost",
      "quality tradeoff"
    ],
    "difficulty": "easy",
    "domain_tags": ["voyage", "embeddings", "rag"]
  },
  {
    "text": "Why would Darwin precompute embeddings for every chunk instead of embedding during each genome evaluation?",
    "ground_truth": "Precomputing embeddings removes repeated embedding latency and cost from each evaluation. It lets many genomes compare retrieval strategies over the same chunk vectors without recomputing document embeddings.",
    "expected_facts": [
      "precompute embeddings",
      "avoid repeated latency",
      "avoid repeated cost",
      "same chunk vectors",
      "compare retrieval strategies"
    ],
    "difficulty": "easy",
    "domain_tags": ["voyage", "embeddings", "rag"]
  },
  {
    "text": "How does a reranker like Voyage rerank-2 improve a RAG pipeline after vector search?",
    "ground_truth": "Vector search retrieves an initial candidate set, and the reranker rescores those candidates against the query with a stronger relevance model. The final context can use the reranked order rather than the raw nearest-neighbor order.",
    "expected_facts": [
      "initial candidate set",
      "reranker rescores",
      "query relevance",
      "stronger relevance model",
      "reranked order"
    ],
    "difficulty": "medium",
    "domain_tags": ["voyage", "embeddings", "rag"]
  },
  {
    "text": "When would multi-query retrieval help more than a single embedded user question?",
    "ground_truth": "Multi-query retrieval helps when the original question is ambiguous or could be phrased several ways. Embedding multiple rewritten queries can retrieve complementary evidence that a single query might miss.",
    "expected_facts": [
      "ambiguous question",
      "multiple rewritten queries",
      "complementary evidence",
      "single query might miss",
      "retrieve broader context"
    ],
    "difficulty": "medium",
    "domain_tags": ["voyage", "embeddings", "rag"]
  },
  {
    "text": "How does chunk size affect embedding-based retrieval quality?",
    "ground_truth": "Smaller chunks can produce focused matches but may lose surrounding context. Larger chunks preserve context but can dilute the exact signal the query vector is trying to match.",
    "expected_facts": [
      "smaller chunks",
      "focused matches",
      "lose context",
      "larger chunks",
      "preserve context",
      "dilute signal"
    ],
    "difficulty": "easy",
    "domain_tags": ["voyage", "embeddings", "rag"]
  },
  {
    "text": "Why should Darwin store embeddings from multiple Voyage model variants for the same chunk?",
    "ground_truth": "Different genomes can choose different embedding models as part of their retrieval genes. Storing several variants lets the system compare quality, latency, and cost tradeoffs without re-embedding the corpus during evaluation.",
    "expected_facts": [
      "different genomes",
      "retrieval genes",
      "multiple model variants",
      "quality tradeoffs",
      "latency tradeoffs",
      "without re-embedding"
    ],
    "difficulty": "medium",
    "domain_tags": ["voyage", "embeddings", "rag"]
  },
  {
    "text": "What is HyDE retrieval and why might an evolved genome choose it?",
    "ground_truth": "HyDE asks a model to draft a hypothetical answer or document, embeds that generated text, and uses it for retrieval. A genome might choose it when the generated answer is closer to relevant documents than the raw user query.",
    "expected_facts": [
      "hypothetical answer",
      "embed generated text",
      "retrieval query",
      "closer to relevant documents",
      "raw user query"
    ],
    "difficulty": "hard",
    "domain_tags": ["voyage", "embeddings", "rag"]
  },
  {
    "text": "How should Darwin account for embedding and reranking latency in a genome's fitness score?",
    "ground_truth": "The judge can score answer quality separately from operational costs, then apply latency or cost penalties to the composite fitness. This prevents a slow high-quality genome from always beating a slightly weaker but much faster one.",
    "expected_facts": [
      "answer quality",
      "latency penalty",
      "cost penalty",
      "composite fitness",
      "slow high-quality genome",
      "faster genome tradeoff"
    ],
    "difficulty": "hard",
    "domain_tags": ["voyage", "embeddings", "rag"]
  },
  {
    "text": "How would LangGraph help orchestrate Darwin's retrieval, judging, and evolution steps?",
    "ground_truth": "LangGraph can model the workflow as stateful nodes and edges, such as retrieve, answer, judge, persist fitness, and evolve. This makes branching, retries, and long-running agent state easier to control.",
    "expected_facts": [
      "stateful nodes",
      "edges",
      "retrieve",
      "judge",
      "persist fitness",
      "long-running state"
    ],
    "difficulty": "hard",
    "domain_tags": ["langchain", "agents"]
  },
  {
    "text": "What should an agent state object carry during one Darwin evaluation run?",
    "ground_truth": "It should carry the query, genome configuration, retrieved chunks, generated answer, judge components, fitness score, and trace metadata. That state makes the run reproducible and debuggable.",
    "expected_facts": [
      "query",
      "genome configuration",
      "retrieved chunks",
      "generated answer",
      "judge components",
      "trace metadata"
    ],
    "difficulty": "easy",
    "domain_tags": ["langchain", "agents"]
  },
  {
    "text": "When should a Darwin agent call a retrieval tool instead of answering from memory?",
    "ground_truth": "The agent should retrieve when the query needs factual grounding, current corpus evidence, citations, or domain-specific details. Answering from memory is risky for technical documentation questions.",
    "expected_facts": [
      "factual grounding",
      "corpus evidence",
      "citations",
      "domain-specific details",
      "technical documentation"
    ],
    "difficulty": "easy",
    "domain_tags": ["langchain", "agents"]
  },
  {
    "text": "How can a blackboard pattern make multi-agent retrieval easier to observe?",
    "ground_truth": "Agents can publish retrieved evidence, confidence, proposed answers, and votes into a shared state. The demo can then show who contributed what and how consensus formed.",
    "expected_facts": [
      "shared state",
      "retrieved evidence",
      "confidence",
      "proposed answers",
      "votes",
      "consensus"
    ],
    "difficulty": "medium",
    "domain_tags": ["langchain", "agents"]
  },
  {
    "text": "How do retriever abstractions help Darwin swap retrieval strategies between genomes?",
    "ground_truth": "A common retriever interface lets genomes vary implementation details like top_k, filters, query transforms, and reranking while the agent runner calls the same API. That keeps evolution focused on configuration instead of branching code paths everywhere.",
    "expected_facts": [
      "common retriever interface",
      "top_k",
      "filters",
      "query transforms",
      "reranking",
      "same API"
    ],
    "difficulty": "medium",
    "domain_tags": ["langchain", "agents"]
  },
  {
    "text": "Why is memory dangerous in an evaluation benchmark if agents can see answers from previous runs?",
    "ground_truth": "Shared or persistent memory can leak ground truth or previous judged answers into later evaluations. That makes fitness scores unfair because genomes are no longer being compared on the same clean retrieval task.",
    "expected_facts": [
      "persistent memory",
      "leak ground truth",
      "previous judged answers",
      "unfair fitness scores",
      "same clean retrieval task"
    ],
    "difficulty": "hard",
    "domain_tags": ["langchain", "agents"]
  },
  {
    "text": "How should Darwin capture an agent's tool-use trace for the demo?",
    "ground_truth": "Record each retrieval call, parameters, returned chunk IDs, scores, reranking decisions, and final answer. The UI or terminal demo can replay this trace to explain why a genome won.",
    "expected_facts": [
      "retrieval call",
      "parameters",
      "chunk IDs",
      "scores",
      "reranking decisions",
      "replay trace"
    ],
    "difficulty": "medium",
    "domain_tags": ["langchain", "agents"]
  },
  {
    "text": "How does Voyage's reranker improve recall when used with MongoDB Atlas Vector Search compared to a no-rerank baseline?",
    "ground_truth": "Voyage rerank-2 reorders the top-K results from $vectorSearch by relevance, which typically improves recall@5 by 10-20% on technical RAG benchmarks. The pattern is to overfetch (e.g., numCandidates=200, limit=50 from Atlas) then rerank to a smaller final K, balancing recall against latency.",
    "expected_facts": [
      "voyage rerank-2",
      "$vectorSearch",
      "numCandidates",
      "limit",
      "recall improvement",
      "overfetch then rerank"
    ],
    "difficulty": "hard",
    "domain_tags": ["mongodb", "voyage", "vector-search", "rag"]
  },
  {
    "text": "How would a LangChain retriever wrap Voyage embeddings to query a MongoDB Atlas vector index, and what abstraction boundaries does that create?",
    "ground_truth": "LangChain wraps the Voyage embedding model behind an Embeddings interface and exposes the Atlas index through MongoDBAtlasVectorSearch as a VectorStore. The retriever method calls embed_query on the Voyage client, then issues a $vectorSearch aggregation against the configured index. This decouples the embedding choice, the storage backend, and the agent's retrieval call so a genome can swap any layer without touching the others.",
    "expected_facts": [
      "Embeddings interface",
      "MongoDBAtlasVectorSearch VectorStore",
      "embed_query",
      "$vectorSearch aggregation",
      "decoupled abstraction layers",
      "swap embedding or backend"
    ],
    "difficulty": "hard",
    "domain_tags": ["langchain", "voyage", "mongodb", "rag"]
  },
  {
    "text": "How can a LangGraph agent persist intermediate retrieval results in MongoDB so a long-running evolution loop can resume after a crash?",
    "ground_truth": "LangGraph supports a checkpointer interface; a MongoDB-backed checkpointer writes node state (query, retrieved chunks, partial answer, judge inputs) to a checkpoints collection keyed by thread_id. On restart, the graph rehydrates from the latest checkpoint and resumes at the next pending node, so an interrupted evaluation does not need to re-run retrieval or re-embed the query.",
    "expected_facts": [
      "LangGraph checkpointer",
      "MongoDB checkpoints collection",
      "thread_id key",
      "rehydrate node state",
      "resume pending node",
      "avoid re-running retrieval"
    ],
    "difficulty": "hard",
    "domain_tags": ["langchain", "mongodb", "agents"]
  },
  {
    "text": "Why does MongoDB Atlas Vector Search use a SQL query planner to execute $vectorSearch?",
    "ground_truth": "MongoDB Atlas Vector Search does NOT use a SQL query planner. It uses MongoDB's aggregation framework, with $vectorSearch implemented natively against vector indexes (HNSW or quantized variants). The query planner is the standard MongoDB query planner adapted for vector operations, not a SQL one.",
    "expected_facts": [
      "does not use SQL",
      "aggregation framework",
      "$vectorSearch native",
      "MongoDB query planner",
      "HNSW or quantized indexes"
    ],
    "difficulty": "hard",
    "domain_tags": ["mongodb", "vector-search", "adversarial"]
  },
  {
    "text": "Since Voyage embeddings are generated by a fine-tuned BERT-base encoder, how do I access the raw attention weights for interpretability?",
    "ground_truth": "The premise is wrong on two counts. Voyage does not publish that its production embedding models are fine-tuned BERT-base, and the API only returns vectors, not attention weights or any other internal model state. A correct answer should reject the assumption and note that interpretability for closed embedding APIs is limited to vector-space analyses (cosine neighborhoods, probing classifiers) rather than attention inspection.",
    "expected_facts": [
      "premise unverified",
      "no published BERT-base architecture claim",
      "API returns vectors only",
      "no attention weights exposed",
      "vector-space analyses instead"
    ],
    "difficulty": "hard",
    "domain_tags": ["voyage", "embeddings", "adversarial"]
  },
  {
    "text": "What is the boiling point of water at sea level in Celsius?",
    "ground_truth": "This question is outside the scope of the Darwin technical documentation corpus, which covers MongoDB Atlas Vector Search, Voyage AI embeddings, and LangChain. The retrieved chunks should not contain a confident answer, and a well-behaved agent should explicitly note the absence of relevant context rather than answer from world knowledge.",
    "expected_facts": [
      "outside corpus scope",
      "no relevant context",
      "explicitly refuse",
      "no confident answer"
    ],
    "difficulty": "hard",
    "domain_tags": ["adversarial", "off-corpus"]
  },
  {
    "text": "Who won the 2025 FIFA Club World Cup and what was the final score?",
    "ground_truth": "This question is unrelated to the Darwin corpus, which contains only MongoDB Atlas Vector Search, Voyage AI, and LangChain documentation. Retrieval should surface no relevant chunks, and the agent should explicitly decline to answer rather than hallucinate sports results from parametric memory.",
    "expected_facts": [
      "unrelated to corpus",
      "no retrieved evidence",
      "decline to answer",
      "do not hallucinate from memory"
    ],
    "difficulty": "hard",
    "domain_tags": ["adversarial", "off-corpus"]
  },
  {
    "text": "how do u creat a vector idx in atls for embeddings stored under a nested feild",
    "ground_truth": "Use createSearchIndex with definition.fields containing a vector type entry whose path points to the nested field (e.g. 'embeddings.voyage_4_large'), specifying numDimensions matching the embedding model and similarity (cosine, euclidean, or dotProduct).",
    "expected_facts": [
      "createSearchIndex",
      "definition.fields",
      "type vector",
      "nested path",
      "numDimensions",
      "similarity"
    ],
    "difficulty": "medium",
    "domain_tags": ["mongodb", "vector-search", "robustness"]
  },
  {
    "text": "whats teh diff btwn voyage-3 and voyage-3-lite for embedign latancy in a rag pipline",
    "ground_truth": "voyage-3 is the higher-quality general-purpose embedding model and voyage-3-lite is a smaller, faster, cheaper variant. In a RAG pipeline, voyage-3-lite reduces per-query embedding latency and cost at the expense of some retrieval quality, which can matter when the corpus is small or queries are simple. Genomes that prioritize throughput should prefer the lite variant.",
    "expected_facts": [
      "voyage-3",
      "voyage-3-lite",
      "lite is smaller and faster",
      "lower latency and cost",
      "quality tradeoff",
      "throughput priority"
    ],
    "difficulty": "medium",
    "domain_tags": ["voyage", "embeddings", "rag", "robustness"]
  },
  {
    "text": "Walk through the full pipeline of a Voyage-embedded MongoDB Atlas Vector Search query, from raw query string to ranked chunks, including all transformations and the role of numCandidates.",
    "ground_truth": "The raw query string is sent to Voyage's embed endpoint with input_type='query', which returns a fixed-dimension vector matching the model used to embed corpus chunks. That vector is passed as queryVector inside a $vectorSearch aggregation stage that names the Atlas vector index and the embedding path. Atlas runs an approximate nearest-neighbor scan over its HNSW (or quantized) index, considering numCandidates documents as the search frontier and returning the top limit results by similarity. numCandidates controls recall: larger values explore more of the index graph at the cost of latency, while limit caps the final returned set. Optional downstream stages (filters, reranker, projection) shape the ranked chunks before the agent consumes them.",
    "expected_facts": [
      "Voyage embed input_type=query",
      "matching dimensions across corpus and query",
      "$vectorSearch with queryVector and path",
      "HNSW or quantized index ANN",
      "numCandidates controls recall vs latency",
      "limit caps returned set",
      "optional rerank or filter stages"
    ],
    "difficulty": "hard",
    "domain_tags": ["mongodb", "voyage", "vector-search", "rag"]
  },
  {
    "text": "Compare LangGraph state management with naive context-window concatenation for long-running agent conversations, including memory implications.",
    "ground_truth": "Naive concatenation appends every turn into a growing prompt, which inflates token cost linearly, eventually overflows the context window, and makes it hard to branch or replay. LangGraph instead models the conversation as a typed state object passed between nodes, with explicit reducers that decide what to keep, summarize, or drop. State is checkpointable, so memory can be externalized to a store (e.g., MongoDB) keyed by thread_id rather than re-sent every turn. This bounds prompt size, enables structured short-term vs long-term memory separation, and lets the graph fork or resume from any checkpoint without replaying the full transcript.",
    "expected_facts": [
      "naive concat grows tokens linearly",
      "context window overflow",
      "LangGraph typed state with reducers",
      "checkpointable external memory",
      "bounded prompt size",
      "short-term vs long-term separation",
      "fork or resume from checkpoint"
    ],
    "difficulty": "hard",
    "domain_tags": ["langchain", "agents", "rag"]
  },
  {
    "text": "Explain how MongoDB change streams enable event-driven evolution loops without polling, and what guarantees they provide on at-least-once delivery.",
    "ground_truth": "A change stream opens a cursor on a collection (or database) backed by the replica set oplog and pushes change events to the consumer as they are committed. Darwin can watch the fitness_evaluations collection so the conductor reacts the moment a generation's evaluations land, without scheduled polling. Each event carries a resume token; if the consumer crashes, it reopens the stream with the last persisted token to receive any events emitted since, giving at-least-once delivery. Because retries can replay events after a token, downstream handlers must be idempotent (e.g., upsert by genome_id and generation), and the stream depends on majority read concern and an active oplog window large enough to cover consumer downtime.",
    "expected_facts": [
      "change stream cursor on oplog",
      "watch fitness_evaluations",
      "no polling needed",
      "resume token persistence",
      "at-least-once delivery",
      "idempotent downstream handlers",
      "oplog window must cover downtime"
    ],
    "difficulty": "hard",
    "domain_tags": ["mongodb", "agents"]
  }
]
